.venv/
__pycache__/
*.pyc
checkpoints/
*.npz
dist/
.pytest_cache/

# local-only design notes / plans — never committed or pushed
docs/
# corpus-specific one-off glue + data dumps + checkpoints — not committed for now
recipes/
